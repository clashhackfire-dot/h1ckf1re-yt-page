{
  "name": "pulsetube-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for PulseTube YouTube downloader",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
